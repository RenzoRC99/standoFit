package com.standofit.back.shared.domain.valueobjects.ids;

import com.standofit.back.shared.domain.valueobjects.Id;
import java.util.UUID;

public class SessionDayId extends Id {
  public SessionDayId(UUID value) {
    super(value);
  }
}